[project]
name = "bench-analysis-workbench"
version = "0.1.0"
description = "Benchmark paper analysis pipeline and internal web workbench."
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
bench-analysis = "bench_analysis.cli:main"

[tool.setuptools]
packages = ["bench_analysis"]

